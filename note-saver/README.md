# Note Saver CLI

A simple CLI tool to save and read text notes stored in a local JSON file.

## Usage

```bash
# Save a note
node index.js save "Your note text here"

# Read all saved notes
node index.js read
```
